using System;
using TechTalk.SpecFlow;

namespace SpecFlowProject1.StepDefinitions
{
    [Binding]
    public class AjusteDelTamanoDeTextoStepDefinitions
    {
        [When(@"selecciona un tamaño de texto personalizado")]
        public void WhenSeleccionaUnTamanoDeTextoPersonalizado()
        {
            throw new PendingStepException();
        }

        [Then(@"todos los textos de la plataforma deben cambiar de tamaño acorde a la selección")]
        public void ThenTodosLosTextosDeLaPlataformaDebenCambiarDeTamanoAcordeALaSeleccion()
        {
            throw new PendingStepException();
        }

        [Given(@"que el usuario ajusta el tamaño de texto")]
        public void GivenQueElUsuarioAjustaElTamanoDeTexto()
        {
            throw new PendingStepException();
        }

        [When(@"accede a la plataforma desde diferentes dispositivos")]
        public void WhenAccedeALaPlataformaDesdeDiferentesDispositivos()
        {
            throw new PendingStepException();
        }

        [Then(@"el ajuste de texto debe aplicarse correctamente en todos los dispositivos")]
        public void ThenElAjusteDeTextoDebeAplicarseCorrectamenteEnTodosLosDispositivos()
        {
            throw new PendingStepException();
        }
    }
}
