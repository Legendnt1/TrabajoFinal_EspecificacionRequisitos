using System;
using TechTalk.SpecFlow;

namespace SpecFlowProject1.StepDefinitions
{
    [Binding]
    public class AyudaContextualEnElementosInteractivosStepDefinitions
    {
        [Given(@"que el usuario está en cualquier página con botones interactivos")]
        public void GivenQueElUsuarioEstaEnCualquierPaginaConBotonesInteractivos()
        {
            throw new PendingStepException();
        }

        [When(@"pasa el cursor sobre un botón o ícono")]
        public void WhenPasaElCursorSobreUnBotonOIcono()
        {
            throw new PendingStepException();
        }

        [Then(@"debe aparecer una ayuda contextual breve describiendo la acción que realiza")]
        public void ThenDebeAparecerUnaAyudaContextualBreveDescribiendoLaAccionQueRealiza()
        {
            throw new PendingStepException();
        }

        [Given(@"que el usuario completa un formulario")]
        public void GivenQueElUsuarioCompletaUnFormulario()
        {
            throw new PendingStepException();
        }

        [When(@"enfoca un campo de entrada")]
        public void WhenEnfocaUnCampoDeEntrada()
        {
            throw new PendingStepException();
        }

        [Then(@"debe aparecer una descripción sobre el tipo de información que debe ingresar")]
        public void ThenDebeAparecerUnaDescripcionSobreElTipoDeInformacionQueDebeIngresar()
        {
            throw new PendingStepException();
        }
    }
}
