using System;
using TechTalk.SpecFlow;

namespace SpecFlowProject1.StepDefinitions
{
    [Binding]
    public class AccesoALaPlataformaDesdeDiferentesDispositivosStepDefinitions
    {
        [Given(@"que el usuario registrado quiere usar la plataforma desde diferentes dispositivos")]
        public void GivenQueElUsuarioRegistradoQuiereUsarLaPlataformaDesdeDiferentesDispositivos()
        {
            throw new PendingStepException();
        }

        [When(@"el usuario inicia sesión desde su laptop")]
        public void WhenElUsuarioIniciaSesionDesdeSuLaptop()
        {
            throw new PendingStepException();
        }

        [When(@"luego intenta acceder desde su móvil sin cerrar la sesión en la laptop")]
        public void WhenLuegoIntentaAccederDesdeSuMovilSinCerrarLaSesionEnLaLaptop()
        {
            throw new PendingStepException();
        }

        [Then(@"la sesión en ambos dispositivos está sincronizada y el usuario puede continuar con las mismas actividades")]
        public void ThenLaSesionEnAmbosDispositivosEstaSincronizadaYElUsuarioPuedeContinuarConLasMismasActividades()
        {
            throw new PendingStepException();
        }

        [Given(@"que el usuario registrado inicia sesión en la plataforma desde su laptop")]
        public void GivenQueElUsuarioRegistradoIniciaSesionEnLaPlataformaDesdeSuLaptop()
        {
            throw new PendingStepException();
        }

        [When(@"cambia a su teléfono móvil")]
        public void WhenCambiaASuTelefonoMovil()
        {
            throw new PendingStepException();
        }

        [Then(@"la interfaz de usuario debe adaptarse automáticamente al tamaño de pantalla y las funcionalidades deben estar intactas")]
        public void ThenLaInterfazDeUsuarioDebeAdaptarseAutomaticamenteAlTamanoDePantallaYLasFuncionalidadesDebenEstarIntactas()
        {
            throw new PendingStepException();
        }
    }
}
